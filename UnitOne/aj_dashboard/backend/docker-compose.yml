version: '3.9'

services:
  ajinomatrix-backend:
    image: ajinomatrix-backend:0.0.1
    build: 
      context: . 
      dockerfile: Dockerfile-backend
    networks:
      default:
        ipv4_address: 172.171.0.20
    ports:
      - "8000:8000"
networks:
  default:
    name: ajinomatrix-stack1
    external: true