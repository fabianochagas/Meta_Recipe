version: '3.9'

services:
  ajinomatrix-frontend:
    image: ajinomatrix-frontend:0.0.1
    build: 
      context: . 
      dockerfile: Dockerfile-frontend
    networks:
      default:
        ipv4_address: 172.171.0.21
    ports:
      - "3000:3000"

networks:
  default:
    name: ajinomatrix-stack1
    external: true